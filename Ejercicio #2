void main() {

  print('Este programa suma todos los numeros de una lista');
  List<int> numeros = [5, 15, 25, 35, 45, 55, 120];
  int suma = 0;

  for (int i = 0; i < numeros.length; i++) {
    suma = suma + numeros[i];
  }

  print('La suma de los números en la lista es: $suma');
}
